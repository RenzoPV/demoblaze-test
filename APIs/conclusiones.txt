- Se implementó correctamente una prueba funcional automatizada que valida el flujo completo de gestión de mascotas en la API de PetStore y se cumplió con todos los pasos solicitados.

- La automatización permitió crear una mascota de manera dinámica, consultar su información por ID y actualizar su estatus a “sold”, lo cual asegura que el escenario no dependa de datos preexistentes y validando el comportamiento real de la API.

- La prueba validó tanto el código de respuesta HTTP como el contenido de los datos retornados (IDs y status), lo que garatinza la correcta ejecución de cada operación.

- Se generó un reporte de ejecución que permite visualizar claramente los pasos ejecutados y los resultados obtenidos de cada consulta gracias a Karate DSL.